use crate::defs::{Args, Entry};

fn sort_entires_by_name(entries: Vec<Entry>) {
    todo!()
}

fn sort_entires_by_size(entries: Vec<Entry>) {
    todo!()
}

fn sort_entires_by_type(entries: Vec<Entry>) {
    todo!()
}

pub fn sort_entires(entries: Vec<Entry>, args: &Args) -> Vec<Entry> {
    todo!()
}
